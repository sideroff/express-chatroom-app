extends _layout.pug
block content
    if room
        span room name: #{room.name}
        span room creator: #{room.author.username}
        ul(id="chat")
    form(id="msgForm" data-userId=currentUser._id data-roomId=room._id)
        input(id="msgField" placeholder="Say something!")
        input(type="submit")